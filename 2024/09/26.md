## やったこと
-【JS】ガチで学びたい人のためのJavaScriptメカニズム ~Sec13 視聴中
- 英語音読 0.5h

## わかったこと
- オブジェクトを格納した変数はオブジェクトの参照を格納しているため下記のコードのように格納してから値を変更しても値は変更される。
```
const map = new Map();
const obj = {a: 1}
map.set(0, obj);
obj.a = 2;
console.log(map.get(0)); // => {a: 2}
```

## 感じたこと
- 残業はしんどい

## 学習時間
1h